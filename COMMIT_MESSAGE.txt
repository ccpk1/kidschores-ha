fix: Form data preservation, icons.json translation, entity cleanup

Form Data Preservation (suggested_values pattern):
- Points, Kids, Parents, Chores, Rewards, Bonuses, Penalties, Achievements
- Schema builders now use static empty defaults
- add_suggested_values_to_schema() shows current values in edit forms
- Clearing optional fields now properly saves empty values

Icons Translation (icons.json):
- All entity icons now defined in icons.json for HA frontend translation
- State-based icons for chore/reward status sensors
- Range-based icons for points sensor (0→50→100)
- Removed hardcoded DEFAULT_*_ICON constants from const.py
- Sensors return None to fallback to icons.json

Entity & Device Cleanup Hygiene:
- _remove_device_from_registry() removes kid device on deletion
- _remove_conditional_shadow_kid_entities() on flag changes
- _remove_orphaned_manual_adjustment_buttons() on config change
- _remove_orphaned_shadow_kid_buttons() for disallowed entities
- All removal methods now check config_entry_id (safer than platform)

Other Changes:
- Removed redundant enable_notifications field (derived from service)
- Reward states: locked/available/requested/approved (was not_claimed/claimed)
- Entity selection lists now sorted alphabetically
- UID suffix migration for shadow kid entity gating

name: HACS Action

on:
  push:
  pull_request:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  validate-hacs:
    runs-on: "ubuntu-latest"
    # Skip HACS validation on forks - only validate on main repo
    if: github.repository == 'ad-ha/kidschores-ha' && (github.ref == 'refs/heads/main' || github.event_name == 'pull_request')
    steps:
      - uses: "actions/checkout@v3"
      - name: HACS validation
        uses: "hacs/action@main"
        with:
          category: integration
